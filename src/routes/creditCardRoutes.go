package routes

import (
	// "github.com/adrimm6661604086/TPV_Bank-Simulator/src/controllers"
	"github.com/gin-gonic/gin"
)

func CreditCardRoutes(router *gin.Engine) {
	// cardGroup := router.Group("/cards")
	// {
	// 	cardGroup.GET("/", controllers.GetAllCards)
	// 	cardGroup.POST("/", controllers.CreateCard)
	// 	cardGroup.GET("/:id", controllers.GetCardByID)
	// 	cardGroup.PUT("/:id", controllers.UpdateCardByID)
	// 	cardGroup.DELETE("/:id", controllers.DeleteCard)
	// }
}
